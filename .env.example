# Postgres connection strings (Neon recommended).
# Use the pooled connection for DATABASE_URL and the direct connection for migrations.
DATABASE_URL="postgresql://user:password@ep-your-neon-url.neon.tech/db?sslmode=require&pgbouncer=true&connection_limit=1"
DIRECT_DATABASE_URL="postgresql://user:password@ep-your-neon-url.neon.tech/db?sslmode=require"

# Optional OpenFIGI API key to improve rate limits (20 req/min).
OPENFIGI_API_KEY=
# Overrides allowed origins for the API server (comma separated).
CLIENT_ORIGIN=http://localhost:3000
# Enable to fall back to Stooq EOD data if Yahoo Finance fails.
ENABLE_STOOQ_FALLBACK=false
